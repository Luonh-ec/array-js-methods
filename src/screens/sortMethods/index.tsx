import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SortMethodScreen = () => {
  return (
    <View>
      <Text>SortMethodScreen</Text>
    </View>
  );
};

export default SortMethodScreen;

const styles = StyleSheet.create({});
